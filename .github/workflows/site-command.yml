name: ROAD TO P1 Site Command (legacy)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy bridge disabled. Exact text commands use site-command-core.yml."
